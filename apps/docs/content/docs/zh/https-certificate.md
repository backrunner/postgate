---
title: HTTPS 与根证书
description: 在开发机上信任 PostGate CA，以安全地检查 HTTPS 流量。
navTitle: HTTPS 证书
order: 3
---

# HTTPS 与根证书

为了捕获 HTTPS 请求并应用规则，PostGate 会在本地解密流量。应用会创建一套私有根证书，并在收到请求时为目标域名生成短期证书。

## 安装证书

1. 打开 **Settings → HTTPS & Security**。
2. 点击 **Install to System**。
3. 批准操作系统的信任提示。
4. 重新启动已经打开的浏览器。

如果自动安装不可用，点击 **Export**，将 `.pem` 或 `.crt` 作为受信任的根证书机构导入系统或浏览器证书库。

## 验证 HTTPS 捕获

启动代理，将浏览器指向 `127.0.0.1:8899`，再打开 HTTPS 页面。页面不应出现证书警告，请求也应显示在 Capture 中。选择请求可以查看 TLS 信息、请求与响应头、正文和耗时。

## 安全边界

导出的根证书本身不需要保密，但 PostGate 配置档案可能包含对应的私钥，因此配置档案必须按敏感凭证处理。

- 只在自己控制的开发机上安装 CA。
- 不要分享包含证书材料的配置档案。
- 不再使用 PostGate 时，从系统信任库删除 CA。
- 除非已经设计可信网络方案，否则保持代理只监听本机。

证书错误通常来自浏览器使用独立证书库、CA 未设为受信任，或浏览器尚未重启。可参考[故障排查](/docs/troubleshooting)。
